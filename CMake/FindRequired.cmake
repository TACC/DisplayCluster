
# generated by Buildyard, do not edit. Sets FOUND_REQUIRED if all required
# dependencies are found. Used by Buildyard.cmake
set(FIND_REQUIRED_FAILED)
find_package(Boost 1.41.0 COMPONENTS date_time serialization unit_test_framework QUIET)
if(NOT Boost_FOUND AND NOT BOOST_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} Boost")
endif()
find_package(LibJpegTurbo 1.2.1 QUIET)
if(NOT LibJpegTurbo_FOUND AND NOT LIBJPEGTURBO_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} LibJpegTurbo")
endif()
find_package(FFMPEG  QUIET)
if(NOT FFMPEG_FOUND AND NOT FFMPEG_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} FFMPEG")
endif()
find_package(OpenGL  QUIET)
if(NOT OpenGL_FOUND AND NOT OPENGL_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} OpenGL")
endif()
find_package(Qt4 4.6 COMPONENTS QtNetwork QtOpenGL QtXml QtXmlPatterns QUIET)
if(NOT Qt4_FOUND AND NOT QT4_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} Qt4")
endif()
find_package(TUIO 1.4 QUIET)
if(NOT TUIO_FOUND AND NOT TUIO_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} TUIO")
endif()
find_package(GLUT  QUIET)
if(NOT GLUT_FOUND AND NOT GLUT_FOUND)
  set(FIND_REQUIRED_FAILED "${FIND_REQUIRED_FAILED} GLUT")
endif()
if(FIND_REQUIRED_FAILED)
  set(FOUND_REQUIRED FALSE)
else()
  set(FOUND_REQUIRED TRUE)
endif()
